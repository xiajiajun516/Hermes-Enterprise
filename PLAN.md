# Plan — v1.4.0

The static v1.4 implementation establishes a forward-only Contract parser, immutable exclusive writer, lineage validator, tracked-manifest health gate, and fixture coverage. A real post-bootstrap run must create and track its own Contract, outputs, and manifest before health can pass. No legacy artifact handling, compatibility layer, migration, or Git move is planned.
