# OS generated files
.DS_Store
Thumbs.db
# Environment and sensitive data
.env
.env.*
*.pem
*.key
# Stage deliverables (spec.md / report.md / review.md) are TRACKED — never ignore
# Temporary artifacts & build outputs
dist/
build/
node_modules/
__pycache__/
*.pyc
# Local session databases
*.db
*.sqlite
# Coverage artifacts
.coverage
htmlcov/
coverage.xml
# Local planning documents (not part of the shipped profile)
docs/
# Scratch handoff files written by subagents nearing their tool budget
artifacts/handoffs/
